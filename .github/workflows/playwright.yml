name: Playwright CI/CD - Naukri

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Cache Node.js dependencies
      - name: Cache Node.js modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Install Playwright browsers
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # 6️⃣ Create .env file dynamically
      - name: Create .env
        run: |
          echo "NAUKRI_EMAIL=${{ secrets.NAUKRI_EMAIL }}" >> .env
          echo "NAUKRI_PASSWORD=${{ secrets.NAUKRI_PASSWORD }}" >> .env
          echo "BASE_URL=${{ secrets.BASE_URL }}" >> .env
          echo "CV=${{ secrets.CV }}" >> .env

      # 7️⃣ Run Playwright tests
      - name: Run Playwright tests
        run: npx playwright test --reporter=line

      # 8️⃣ Upload Playwright test results (screenshots, traces, etc.)
      - name: Upload Playwright test results
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: test-results/

      # 9️⃣ Upload Playwright HTML report
      - name: Upload Playwright HTML report
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report/

      # 🔟 Upload CSV report
      - name: Upload CSV Report
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: job_report
          path: job_report.csv
