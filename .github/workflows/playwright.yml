name: Playwright CI/CD - Naukri

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Cache Node.js dependencies
      - name: Cache Node.js dependencies
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Cache Playwright browsers
      - name: Cache Playwright browsers
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      # 6️⃣ Install Playwright browsers
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # 7️⃣ Create .env dynamically
      - name: Create .env
        run: |
          echo "NAUKRI_EMAIL=${{ secrets.NAUKRI_EMAIL }}" >> .env
          echo "NAUKRI_PASSWORD=${{ secrets.NAUKRI_PASSWORD }}" >> .env
          echo "BASE_URL=${{ secrets.BASE_URL }}" >> .env
          echo "CV=${{ secrets.CV }}" >> .env

      # 8️⃣ Run Playwright tests and generate HTML report
      - name: Run Playwright tests
        run: npx playwright test --reporter=html,line

      # 9️⃣ Upload Playwright test results
      - name: Upload Playwright test results
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: test-results/

      # 🔟 Upload job_report.csv
      - name: Upload CSV Report
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: job_report
          path: job_report.csv

      # 1️⃣1️⃣ Prepare HTML report with run folder name
      - name: Move report to run-numbered folder
        if: success() || failure()
        run: |
          mkdir -p "report-history/${{ github.run_number }}"
          cp -r playwright-report/* "report-history/${{ github.run_number }}/"

      # 1️⃣2️⃣ Upload HTML report for GitHub Pages
      - name: Upload Playwright HTML report
        if: success() || failure()
        uses: actions/upload-pages-artifact@v3
        with:
          path: report-history/

  # 1️⃣3️⃣ Deploy HTML report to GitHub Pages
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
